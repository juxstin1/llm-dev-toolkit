# Support

Use GitHub issues for support requests that may uncover bugs or documentation
gaps.

Before opening an issue:

1. Check the README and docs under `docs/`.
2. Run the command with `--help`.
3. Include your operating system, shell, command, expected result, and actual
   output.

For security-sensitive reports, use [SECURITY.md](SECURITY.md) instead of a
public issue.
